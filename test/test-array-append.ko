ko int main() {
    list<node<int>> l;
    node<int> x;
    node<int> y;
    node<int> z;
    int i;

    l = [];

    x = new node{101};
    y = new node{202};
    z = new node{303};

    append(l, x);
    append(l, y);
    append(l, z);

    for (i=0; i < l.length; i = i+1) {
        print(l[i].val);
    }
    
}